-- Esquema de referencia para la base SQLite local.
-- La aplicación lo crea automáticamente al iniciar server.js.
-- Para ver el esquema completo consulte backend/db.js.

-- El archivo de datos se crea en backend/data/gestionbar.sqlite.
-- No se expone directamente a los navegadores: solo se accede mediante /api.
