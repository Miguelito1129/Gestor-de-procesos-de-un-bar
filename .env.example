# Local API (leave empty to use the Vite proxy during development)
VITE_LOCAL_API_URL=

# Optional AI assistant key
# In production, proxy AI requests through a backend.
VITE_ANTHROPIC_KEY=sk-ant-XXXXXXX

# Pepper for local password hashing (use a different value in production)
VITE_APP_PEPPER=change-this-pepper
